[package]
name = "chronographer_unittests"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "../LICENSE"

[dependencies]
chronographer = { path = "../../core" }
chronographer_utils = { path = "../../utils" }
async-trait = "0.1.89"
tokio = { version = "1.52.0", features = ["full", "test-util"] }
trybuild = "1.0"

[dev-dependencies]
tokio = "1.50.0"
trybuild = "1.0"
